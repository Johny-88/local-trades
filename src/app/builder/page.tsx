import type { Metadata } from "next";import { TradeCategoryLandingPage } from "../../components/TradeCategoryLandingPage";import { getTradeCategory } from "../../lib/tradeCategoryContent";import { createPageMetadata } from "../../lib/seo";const trade=getTradeCategory("builder")!;export const metadata:Metadata=createPageMetadata({title:`Find Local ${trade.plural} Near You | Local Trades`,description:trade.hero,path:`/${trade.slug}/`,ogImage:trade.ogImage});export default function Page(){return <TradeCategoryLandingPage trade={trade}/>}
